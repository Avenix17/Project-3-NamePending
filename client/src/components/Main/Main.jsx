import React from "react";

const Main = (props) => {
    return(
        <ApolloProvider client = {client}>
            <section id = "main">
                
            </section>
        </ApolloProvider>
    );
};

export default Main;